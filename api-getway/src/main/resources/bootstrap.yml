spring:
  application:
    name: api-getway
  cloud:
    config:
      discovery:
        service-id: CONFIG
        enabled: true
      profile: dev
zuul:
  routes:
    product: /myProduct/**
  ignored-patterns:
    - /**/product/listForOrder
